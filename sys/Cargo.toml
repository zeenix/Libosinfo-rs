[build-dependencies]
pkg-config = "0.3.7"

[dependencies]
bitflags = "0.9"
libc = "0.2"
glib = "0.3.0"
gio = "0.2.0"

[features]

[lib]
name = "libosinfo_sys"

[package]
build = "build.rs"
links = "libosinfo"
name = "libosinfo-sys"
version = "0.2.0"
